apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spendpulse-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: spendpulse-service
            port:
              number: 80
